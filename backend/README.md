# Hensight Backend

The backend of hensight, handles iteration with TBA, Nexus, and processing all data before handing off to frontned

# Usage

```bash
    #install packages
    pip install -r requirements.txt
    #run program
    pm2 start ecosystem.config.js
```